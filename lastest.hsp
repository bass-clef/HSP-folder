	
	exp = 2
	a = powf(exp, 3) // 80000.0
	
	mes ""+exp+"::"+ strf("%.64g", a) +":"+ basef(a, exp)+":"+(logf(exp)*8)
	
	mes logf(a)*x
	
	// 8 = 2 ^ 3
	// 2 = powf(8, 1/3)
	// 3 = 
	
	#module
	#defcfunc basef double value, double exponent
	return powf(value, 1.0/exponent)
	#global
	
